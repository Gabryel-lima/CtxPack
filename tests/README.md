# Test helpers

This directory contains smoke-test helpers and polyglot fixtures used to validate semantic extraction.

Run the full smoke suite from the repository root:

```bash
python3 tests/run_smoke.py
```

The multi-language fixture set lives in `tests/prototypes/`.